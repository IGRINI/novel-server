-- +migrate Down
-- Удаление значения из ENUM типа является сложной операцией.
-- Это требует сначала миграции всех данных, использующих это значение, на другое.
-- Поэтому автоматический откат этой миграции не предоставляется.
-- Если откат необходим, его следует выполнять вручную с осторожностью.

-- Возможный вариант (ТРЕБУЕТ РУЧНОЙ ПРОВЕРКИ И АДАПТАЦИИ):
-- BEGIN;
-- UPDATE published_stories SET status = 'game_over_fail' WHERE status = 'game_over_pending';
-- -- Здесь должен быть способ удалить значение из ENUM, но стандартный SQL не предоставляет простого DROP VALUE.
-- -- Это может потребовать создания нового типа и пересоздания зависимых объектов.
-- COMMIT; 