-- +migrate Down
-- Внимание: Удаление значения из ENUM в PostgreSQL - сложная операция.
-- Обычно это требует создания нового типа, переноса данных и переименования.
-- В рамках простой миграции откат не предусмотрен.
-- Оставляем пустым или вызываем ошибку, если откат критичен.
-- SELECT 1; -- Пример: ничего не делаем 